version: '3.8'

services:
  webapp:
    build:
      context: .
      dockerfile: ./BlazorApp1/Dockerfile
    ports:
      - "80:8080"
    restart: always

  tgbot:
    build:
      context: .
      dockerfile: ./TgBot/Dockerfile
    restart: always